# Bookmark Sync 详细使用文档

本文档面向第一次使用的用户，从零开始逐步讲解：三种云端方式怎么选、每个平台具体要配置什么、日常怎么用、出问题怎么排查。

---

## 目录

1. [三种同步方式怎么选](#一三种同步方式怎么选)
2. [GitHub 仓库模式配置](#二github-仓库模式配置)
3. [Gitee 仓库模式配置](#三gitee-仓库模式配置)
4. [GitHub Gist 模式配置](#四github-gist-模式配置)
5. [日常使用](#五日常使用)
6. [自动同步详解](#六自动同步详解)
7. [多设备同步的推荐用法](#七多设备同步的推荐用法)
8. [冲突处理详解](#八冲突处理详解)
9. [备份、恢复与清空](#九备份恢复与清空)
10. [换新设备 / 重装浏览器](#十换新设备--重装浏览器)
11. [故障排查对照表](#十一故障排查对照表)
12. [安全须知](#十二安全须知)
13. [已知限制](#十三已知限制)

---

## 一、三种同步方式怎么选

| | GitHub 仓库 | Gitee 仓库 | GitHub Gist |
|---|---|---|---|
| 配置复杂度 | 中（建仓库 + 精细令牌） | 中（建仓库 + 私人令牌） | **最低（只要一个令牌）** |
| 国内访问速度 | 一般 | **快** | 一般 |
| 并发写入保护 | **有**（原子校验） | **有**（原子校验） | 无（先检后写） |
| 同步历史 | 仓库提交历史 | 仓库提交历史 | Gist 修订历史 |
| 适合场景 | 主力方案 | 国内网络环境 | 想最快用起来 |

建议：

- **图省事**：选 GitHub Gist，五分钟配完。
- **多设备频繁同步**：选 GitHub 或 Gitee 仓库模式（写入有原子校验，多设备同时推送更安全）。
- **求稳**：可以同时配置两个平台，弹窗勾选"推送到全部已配置平台"，双云端各存一份。

---

## 二、GitHub 仓库模式配置

### 第 1 步：创建仓库

1. 登录 GitHub，点击右上角 `+` → `New repository`
2. 仓库名随意（如 `bookmark-sync`），可见性务必选 **Private（私有）**
3. 创建后记下三个信息：
   - **owner**：你的用户名（仓库地址 `github.com/<owner>/<repo>` 中的第一段）
   - **repo**：仓库名
   - **branch**：默认分支，新仓库通常是 `main`

> 不需要在仓库里预先创建任何文件，首次推送会自动创建快照文件。

### 第 2 步：创建 Fine-grained Token（推荐）

1. GitHub 右上角头像 → `Settings`
2. 左侧最底部 `Developer settings`
3. `Personal access tokens` → `Fine-grained tokens` → `Generate new token`
4. 依次填写：
   - **Token name**：随意，如 `bookmark-sync`
   - **Expiration**：有效期，到期后需重新生成并更换（建议 90 天以上，自行权衡）
   - **Resource owner**：选择仓库所属的账号
   - **Repository access**：选 `Only select repositories`，然后勾选第 1 步创建的仓库
5. 展开 `Repository permissions`，**点击 `Add permissions` 按钮**逐项添加（直接看列表不点添加是不生效的，这是最常见的坑）：
   - `Metadata` → `Read-only`（必需，有时会自动带上）
   - `Contents` → `Read and write`（必需，读写快照文件靠它）
6. 点击 `Generate token`，**立即复制**（页面关闭后无法再查看）

> 备选：用 classic Token（`Tokens (classic)` → 勾选 `repo` scope）也可以，但权限范围比精细令牌大，不推荐。

### 第 3 步：在扩展中填写

打开扩展弹窗 → 右上角齿轮进入设置页：

| 字段 | 填什么 | 示例 |
|---|---|---|
| 同步提供方 | `GitHub` | — |
| Token | 第 2 步复制的令牌，**直接粘贴原文** | `github_pat_xxx` |
| 用户/组织 | owner | `your-name` |
| 仓库名 | repo | `bookmark-sync` |
| 分支 | 默认分支 | `main` |
| 文件路径 | 快照存放路径，保持默认即可 | `bookmarks/snapshot.json` |

注意：Token 不要带 `Bearer`、`token` 前缀或引号（就算带了扩展也会自动清洗，但建议粘贴原文）。

### 第 4 步：保存、测试、首推

1. 点 `保存设置`
2. 点 `测试连接`，看到"连接成功"即配置无误
3. 回到弹窗，点 `推送`——首次推送会自动在仓库里创建 `bookmarks/snapshot.json`
4. 之后设置页和弹窗会出现"查看云端历史"链接，指向仓库的提交历史页

---

## 三、Gitee 仓库模式配置

### 第 1 步：创建仓库

1. 登录 Gitee，右上角 `+` → `新建仓库`
2. 仓库名随意，**选"私有"**
3. 记下 owner（个人空间地址里的用户名）、仓库名、默认分支（Gitee 新仓库常见 `master`，也可能是 `main`，以仓库页面实际显示为准）

### 第 2 步：创建私人令牌

1. Gitee 右上角头像 → `设置`
2. 左侧 `安全设置` → `私人令牌`
3. `生成新令牌`：
   - 描述随意，如 `bookmark-sync`
   - 权限勾选 **`projects`**（仓库操作权限，读写文件靠它；其余都不需要）
4. 提交后**立即复制令牌**（只显示一次）

### 第 3 步：在扩展中填写

| 字段 | 填什么 | 示例 |
|---|---|---|
| 同步提供方 | `Gitee` | — |
| Token | 私人令牌原文 | `xxxxxxxx` |
| 用户/组织 | owner | `your-name` |
| 仓库名 | repo | `bookmark-sync` |
| 分支 | 默认分支 | `master` |
| 文件路径 | 保持默认 | `bookmarks/snapshot.json` |

### 第 4 步：保存、测试、首推

同 GitHub：`保存设置` → `测试连接` → 弹窗 `推送`。首次推送自动创建快照文件，无需手动建。

---

## 四、GitHub Gist 模式配置

三种方式里最简单的：不用建仓库，只要一个令牌。

### 第 1 步：创建 classic Token

1. GitHub 头像 → `Settings` → `Developer settings`
2. `Personal access tokens` → **`Tokens (classic)`** → `Generate new token (classic)`
3. 勾选 **`gist`** 这一项 scope，其余全不需要
4. 生成后复制

> ⚠️ **必须用 classic 类型**。Fine-grained Token 目前不支持 Gist API，填了会一直报 401/403。

### 第 2 步：在扩展中填写

| 字段 | 填什么 |
|---|---|
| 同步提供方 | `Gist` |
| Token | classic 令牌原文 |
| Gist ID | **留空**（首次推送自动创建私有 Gist，并把 ID 自动回填到这里） |
| 文件名 | 保持默认 `bookmarks.json` |

### 第 3 步：保存、测试、首推

1. `保存设置` → `测试连接`（此时会提示"未填写 Gist ID，首次推送会自动创建"）
2. 弹窗点 `推送` → 扩展自动创建一个**私有 Gist** 并回填 ID
3. 之后"查看云端历史"链接指向该 Gist 的修订历史页

### Gist 模式须知

- 如果你手动填了一个**公开** Gist 的 ID，测试连接会警告，建议改用私有。
- 想"换一个全新的 Gist 重新开始"：清空 Gist ID 字段并保存，下次推送会重新创建。
- 边界：Gist 写入接口没有并发写入保护（不同于仓库模式）。推送前的冲突检测仍然生效，但两台设备**同一瞬间**推送时后写覆盖先写。多设备高频写入建议用仓库模式。

---

## 五、日常使用

### 弹窗（点击工具栏图标）

自上而下：

1. **状态条**：最近同步时间 + 状态圆点
2. **数量卡片**：本地书签数 + 每个已配置平台的云端书签数（未配置的平台不显示）
3. **推送 / 拉取**两颗主按钮：
   - `推送`＝把本地书签写到云端（云端被覆盖）
   - `拉取`＝把云端快照导入本地（本地被覆盖）
   - 按钮作用于"默认平台"，按钮下方小字标注当前目标
4. **高级选项**（折叠区；只配置了一个平台时自动隐藏）：
   - 默认平台下拉：切换推送/拉取的目标平台
   - `推送到全部已配置平台`：勾选后一次推送同时发往所有已配置平台（该选择会保存，自动推送也遵循它）
   - `查看云端历史`

### 首次同步：先想清楚"以哪边为准"

| 场景 | 操作 |
|---|---|
| 这台电脑的书签是最新最全的 | `推送` |
| 云端（另一台设备推的）是最新的 | `拉取` |
| 云端还没有快照 | 只能先 `推送` 一次建立快照 |

### 工具栏徽章含义

| 徽章 | 含义 | 怎么消 |
|---|---|---|
| `…` 灰 | 书签有变更，等防抖窗口结束后自动推送 | 推送完成自动消 |
| `✓` 绿 | 自动推送成功 | 约 30 秒自动消 |
| `!` 橙 | 自动推送遇到冲突被跳过（需人工处理） | 打开弹窗即视为已知晓 |
| `!` 红 | 自动推送失败（网络/令牌问题） | 打开弹窗即视为已知晓 |

---

## 六、自动同步详解

设置页"自动同步"分组里有三个独立开关，可任意组合：

### 1. 书签变更后自动推送（推荐开启）

- 增删改书签后进入**防抖窗口**（默认 60 秒，可配 30–300 秒），窗口内继续改动会合并，窗口结束推送一次
- 拉取/恢复备份导入书签**不会**触发它（内置回环抑制，不会出现拉取完又自动推送的循环）
- 推送失败（如网络抖动）会自动退避重试最多 2 次；遇到冲突则跳过并亮橙色徽章，等你人工处理

### 2. 定时自动推送

- 固定间隔兜底推送，最小 15 分钟
- 与变更触发可同时开启：变更触发管"及时"，定时管"兜底"

### 3. 启动时检查云端更新

- 浏览器启动时对比云端版本，如果云端比本地新（且本地没改过），弹通知提醒 +亮 `…` 徽章
- **只提醒，绝不自动拉取覆盖本地**——是否拉取永远由你决定

### 通知开关（设置页"通知"分组）

| 开关 | 默认 |
|---|---|
| 自动推送失败时通知 | ✅ 开 |
| 自动推送成功时通知 | ❌ 关 |
| 手动操作完成时通知 | ❌ 关 |

---

## 七、多设备同步的推荐用法

假设电脑 A（公司）和电脑 B（家里）：

1. **A 上**：完整配置平台 → 首次 `推送`
2. **A 上**：设置页 `导出配置 JSON`（含令牌，注意保管）
3. **B 上**：安装扩展 → 设置页 `导入配置 JSON` → 弹窗 `拉取`（以云端为准初始化 B）
4. 两台都开启"书签变更后自动推送"和"启动时检查云端更新"
5. 日常习惯：**到另一台设备上开始用之前，先看一眼弹窗**——有"云端有更新"提醒就先 `拉取`，再开始加书签

这样能把冲突概率降到最低。真遇到冲突见下一节。

---

## 八、冲突处理详解

### 什么情况会冲突？

扩展为每个平台记录一份"上次同步基线"。推送/拉取前会做三方对比：

- **推送时**：云端相对基线变了（说明其他设备推过新内容），而你本地也改过 → 冲突。直接推会把别人的改动覆盖掉，所以拦下来。
- **拉取时**：本地相对基线变了（本地有未推送的改动） → 冲突。直接拉会把你本地的新改动覆盖掉，所以拦下来。

### 冲突了怎么办？

弹窗会显示**差异预览**：双方书签数量 + 变更样例。你有三个选择：

1. **取消**，先想清楚
2. 想保留云端 → 执行**拉取并强制**（本地被云端覆盖）
3. 想保留本地 → 执行**推送并强制**（云端被本地覆盖）

强制覆盖前建议先到设置页 `导出书签 JSON` 做个备份。

### 自动推送遇到冲突

自动任务**永远不会强制覆盖**：遇到冲突只会跳过 + 亮橙色 `!` 徽章（+可选通知），等你打开弹窗人工决定方向。

---

## 九、备份、恢复与清空

### 手动备份 / 恢复（设置页"备份与迁移"）

- `导出书签 JSON`：把当前本地书签存成文件，**大动作（强制覆盖、清空）之前建议先导一份**
- `导入书签 JSON`：用备份文件覆盖本地书签（导入过程不会触发自动推送）

### 清空本地书签（设置页"危险操作"）

适合"想让本机完全以云端为准"前的清场。安全流程：

1. 点击后**先自动下载一份备份 JSON**，同时在扩展内保存一份清空前快照（双保险，备份保存成功才会开始删除）
2. 确认框显示将删除的数量，确认后才执行
3. 只清空三个标准书签根（书签栏 / 其他书签 / 移动设备书签），Edge 工作区等特殊目录不受影响
4. 清空**不会**触发自动推送（不会把空书签树推上云端）

误操作：点旁边的 `恢复最近清空的书签` 即可还原；或用下载的备份 JSON 手动导入。

---

## 十、换新设备 / 重装浏览器

扩展配置存在浏览器本地存储里，**卸载扩展通常会连配置一起清掉**。所以：

- 换设备/重装前：设置页 `导出配置 JSON` 保存好
- 新环境：安装扩展 → `导入配置 JSON` → `拉取`
- 云端的快照文件一直都在，书签数据不会因为本地卸载而丢失

---

## 十一、故障排查对照表

| 现象 | 最可能的原因 | 处理 |
|---|---|---|
| 测试连接报 401 | 令牌无效/过期/复制不完整 | 重新生成令牌；确认粘贴的是原文，无 `Bearer` 前缀、引号、换行 |
| GitHub 精细令牌明明建了还是 401/403 | 建令牌时没点 `Add permissions`，权限没真正加上 | 重新编辑令牌，确认 `Metadata: Read-only` 和 `Contents: Read and write` 出现在已添加列表里 |
| Gitee 报 401/403 | 私人令牌没勾 `projects` 权限 | 重新生成令牌并勾选 `projects` |
| Gist 一直 401/403 | 用了 Fine-grained Token | 换 **classic** Token 并勾选 `gist` scope |
| 拉取报"远端文件不存在" | 云端还没有快照 | 先 `推送` 一次 |
| 测试连接报 404 | owner/repo/分支名拼写错误，或令牌没授权到这个仓库 | 对照仓库页面地址逐项核对；精细令牌确认 `Repository access` 里选中了该仓库 |
| 改了书签没自动推送 | 变更开关没开 / 防抖没到 / 冲突被跳过 | 看设置页开关；等防抖秒数；徽章是橙 `!` 就打开弹窗处理冲突 |
| 收不到系统通知 | 通知开关没开，或系统层面禁了浏览器通知 | 检查设置页"通知"分组 + 操作系统的通知权限 |
| 输入 11 分钟被改成 15 | 定时推送最小间隔 15 分钟 | 正常行为 |
| 弹窗数量卡片缺某个平台 | 该平台配置不完整 | 设置页把必填项补齐并保存 |
| 拉取报 `Can't modify workspace folder` | Edge 工作区目录不允许扩展修改 | 已自动跳过非标准目录，只同步三个标准根；如仍报错请反馈 |
| 卸载重装后配置没了 | 卸载清空了浏览器本地存储 | 用之前导出的配置 JSON 导入；以后卸载前先导出 |

---

## 十二、安全须知

1. 仓库/Gist 务必用**私有**——书签快照里有你的全部收藏
2. 令牌只授最小权限：仓库模式只授单仓库的 Contents 读写；Gist 模式只勾 `gist`
3. `导出配置 JSON` 的文件**包含令牌明文**，不要传到公开场合；不用了就删
4. 令牌设置有效期并定期轮换；怀疑泄露立即在平台上撤销
5. 强制覆盖、清空书签前先导出一份书签 JSON

---

## 十三、已知限制

- 自动同步只做"推送"；云端更新只提醒，**不会自动拉取**（防止在你不知情时重写本地书签）
- 拉取一次只能从一个平台执行
- 仓库模式下快照文件超过约 1 MB 时无法读回（对应数万条书签的量级，一般用不到；Gist 模式无此限制）
- Gist 写入无并发保护，多设备同一瞬间推送为"后写覆盖先写"
- 不支持 WebDAV / S3 / Firefox / 按文件夹选择性同步
